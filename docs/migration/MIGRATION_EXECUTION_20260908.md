# 迁移执行记录

- 执行时间：2026-09-08
- 目标：项目版代码切换为安装版运行代码
- 备份目录：`~/.agent/backups/chat-history-migration-20260908-160224`

## 已完成

- 识别并停止原安装版 MCP 进程，释放 17891 端口。
- 完整备份安装版代码、生产 `chat.db`、模型目录、配置和清单。
- 项目版测试：110 tests，0 failures，1 skipped（可选真实 ONNX 模型测试）。
- 影子验证：项目版代码读取生产数据库备份成功；health、schema、FTS、embedding、reranker 均正常；读取到 25 个会话。
- 将项目版运行代码和配置模块部署到安装版。
- 明确未覆盖安装版 `.venv`、`models` 和 `chat.db`。
- 切换后使用安装版虚拟环境读取生产库成功：health 为 `ok`，读取到 25 个会话，recent 返回正常。
- `.zcode/cli/config.json` 仍指向安装版 MCP 入口和安装版 `chat.db`。

## 特别保留

- 安装版原 `title_cache.json` 未被覆盖，仍保留在安装目录；其原始副本也在备份目录。
- 安装版旧 `title_fetcher.py` 未删除，便于回滚和后续清理。

## 待确认项

生产 `chat.db` 目录树在备份快照和切换后检查之间发生了变化：

- 备份快照：13,465 个文件，894,880,321 字节。
- 切换后检查：13,522 个文件，902,586,847 字节。

因此本记录不把生产数据库视为已完成字节级一致性验收。差异可能来自 LanceDB 运行时文件、后台重启或备份期间的并发变化，必须在下一次维护窗口用停止所有相关进程后的稳定快照再次核对；在确认前不要删除备份，也不要清理旧缓存。

## 当前状态

**代码已切换，运行冒烟通过；生产数据库一致性待确认。**

回滚点：使用备份目录恢复安装版代码/缓存，保留现有生产数据库，恢复后执行只读健康检查。
